import { ListEventCapsPipe } from './list-event-caps.pipe';

describe('ListEventCapsPipe', () => {
  it('create an instance', () => {
    const pipe = new ListEventCapsPipe();
    expect(pipe).toBeTruthy();
  });
});
